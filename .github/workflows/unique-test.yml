name: Unique Test - 0x0e
on: [push, workflow_dispatch]

jobs:
  unique-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@main
        continue-on-error: true

      - name: Unique Test Execution - 0x0e
        continue-on-error: true
        run: |
          nix-shell -p curl jq coreutils --run '
            echo "=== Unique Test: 0x0e ===" > unique-test-0x0e.txt
            echo "Repository: ${{ github.repository }}" >> unique-test-0x0e.txt
            echo "Test ID: TEST_008" >> unique-test-0x0e.txt
            echo "Timestamp: $(date)" >> unique-test-0x0e.txt
            curl -s ifconfig.me >> unique-test-0x0e.txt
            echo "Unique instruction: Hex node analysis and coordination testing" >> unique-test-0x0e.txt
          '

      - name: Ubuntu Fallback - 0x0e
        if: failure()
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl jq
          echo "Ubuntu fallback for 0x0e" > fallback-0x0e.txt
          curl -s ifconfig.me >> fallback-0x0e.txt

      - name: Upload Results
        uses: actions/upload-artifact@v4
        with:
          name: unique-test-0x0e
          path: "*.txt"
          retention-days: 2